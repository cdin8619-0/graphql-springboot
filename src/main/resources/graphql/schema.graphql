type Product {
    id: ID,
    name: String,
    Category: String,
    price: Float,
    stock: Int
}

type Query {
    getProducts: Product
}

type Mutation {
    updateStock(id:ID, stock:Int):Product
    receiveNewShipment(id:ID, quantity:Int):Product
}
